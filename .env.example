# Feature flags
# Public feature flags are exposed to the client, so use them for UI and route gating only.
# Defaults can differ by environment. In this repo, docs default to enabled in development
# and disabled in production unless overridden below.

PUBLIC_FEATURE_DOCS=false
PUBLIC_FEATURE_RELEASE_HISTORY=false
